{
  "name": "@n8n/web",
  "version": "0.1.0",
  "private": true,
  "description": "Static web landing page for the n8n monorepo baseline.",
  "type": "module",
  "scripts": {
    "dev": "npm run build && python3 -m http.server 4173 -d dist",
    "build": "rm -rf dist && tsc -p tsconfig.json && cp index.html styles.css dist/",
    "check": "npm run lint && npm run typecheck && npm run test",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^9.24.0",
    "globals": "^16.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.29.0",
    "vitest": "^3.1.1",
    "jsdom": "^26.0.0"
  }
}
