{
  "name": "@n8n/web",
  "version": "0.1.0",
  "private": true,
  "description": "Static web landing page for the n8n monorepo baseline.",
  "scripts": {
    "dev": "python3 -m http.server 4173 -d .",
    "build": "mkdir -p dist && cp index.html styles.css main.js dist/",
    "check": "node --check main.js"
  }
}
